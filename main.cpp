#include <iostream>
#include "DatabaseHandling/databaseConnection.h"
//#include "DatabaseClasses/User.h"
//#include "DatabaseClasses/Forum.h"
//#include "DatabaseClasses/Thread.h"
#include "DatabaseClasses/Post.h"
#include "DatabaseHandling/forumsMaker.h"
#include <time.h>
#include <ctime>

#define LOCAL_IP "192.168.0.57"
#define NORMAL_IP "99.251.104.144"
#define DB "ece656project"

int main() {
    const std::string user = "hammad";
    const std::string password = "hammadtrishal";
    /*struct tm birthDate;
    std::string firstName = "Trishal";
    std::string lastName = "Sudula";
    std::string stateProvince = "ON";
    std::string country = "Canada";
    std::string userID = "2";
    std::vector<std::string> friends = std::vector<std::string>();

    std::string forumID = "1";
    std::string titleID = "tt0000001";
    std::vector<std::string> threads = std::vector<std::string>();
    threads.push_back("1");

    std::string threadID = "1";
    std::vector<std::string> posts = std::vector<std::string>();
    posts.push_back("1");

    std::string postID = "1";
    std::string postText = "Test";


    strptime("1989-11-25 00:00:00", "%Y-%m-%d %H:%M:%S", &birthDate);
    birthDate.tm_isdst = -1;*/

    //User *moo = new User(userID, firstName, lastName, &birthDate, country, stateProvince, friends);
    //Forum *moo = new Forum(forumID,titleID,threads);
    //Thread *moo = new Thread(threadID,forumID,posts);
    //Post *moo = new Post(postID,threadID,userID,postText);





}
